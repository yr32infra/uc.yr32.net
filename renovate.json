{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["static.yml"],
      "matchStrings": ["depName=(?<depName>.*?)?\\s.*?_VERSION=\"(?<currentValue>.*?)\""],
      "versioningTemplate": "docker",
      "datasourceTemplate": "github-releases"
    }
  ]
}
